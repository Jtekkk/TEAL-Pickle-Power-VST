# Assets

The Pickle Power UI is drawn **procedurally** in code (see `Source/GUI/`), so the
plugin does not depend on these files at build time. They are kept here as design
references / starting points for future artwork (and for binary-embedding via
`juce_add_binary_data` if desired later).
